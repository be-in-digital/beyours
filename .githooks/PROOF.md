# Deliberately attributed commit

This file exists only so the commit that adds it has something to carry. The
commit message on it carries the trailers `CLAUDE.md` rule 10 forbids, written
with `--no-verify` so `.githooks/commit-msg` does not strip them, in order to
watch the `Lint` job refuse it. Delete the branch once it has gone red.
