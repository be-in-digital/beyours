# Usage Guide - Convex Functions

## Installing in a Next.js app

### Step 1: Copy the files into the convex/ folder

The files in this package must be copied into the `convex/` folder of your Next.js app.

```bash
# From the project root
cp packages/convex-functions/src/*.ts apps/restaurant-theme/convex/
```

**Note:** Do NOT copy `index.ts`; it is only used for the package export.

### Step 2: Structure of the convex/ folder

After copying, your `convex/` folder should look like this:

```
apps/restaurant-theme/convex/
├── _generated/          # Generated by Convex
├── schema.ts            # Your Convex schema
├── helpers.ts           # Copied from convex-functions
├── stores.ts            # Copied from convex-functions
├── products.ts          # Copied from convex-functions
├── categories.ts        # Copied from convex-functions
├── orders.ts            # Copied from convex-functions
├── kitchenTickets.ts    # Copied from convex-functions
├── payments.ts          # Copied from convex-functions
├── teamMembers.ts       # Copied from convex-functions
├── languages.ts         # Copied from convex-functions
└── translations.ts      # Copied from convex-functions
```

### Step 3: Define the Convex schema

In `apps/restaurant-theme/convex/schema.ts`, define your schema:

```typescript
import { defineSchema, defineTable } from "convex/server"
import { v } from "convex/values"

export default defineSchema({
  stores: defineTable({
    name: v.string(),
    slug: v.string(),
    description: v.optional(v.string()),
    address: v.object({
      street: v.string(),
      city: v.string(),
      postalCode: v.string(),
      country: v.string(),
      latitude: v.optional(v.number()),
      longitude: v.optional(v.number()),
    }),
    phone: v.optional(v.string()),
    email: v.optional(v.string()),
    hours: v.array(v.object({
      day: v.number(),
      open: v.string(),
      close: v.string(),
      isClosed: v.boolean(),
    })),
    status: v.union(
      v.literal("open"),
      v.literal("closed"),
      v.literal("temporarily_unavailable")
    ),
    settings: v.object({
      currency: v.string(),
      timezone: v.string(),
      deliveryEnabled: v.boolean(),
      pickupEnabled: v.boolean(),
      dineInEnabled: v.boolean(),
      minimumOrderAmount: v.optional(v.number()),
      deliveryFee: v.optional(v.number()),
      deliveryRadius: v.optional(v.number()),
      taxRate: v.optional(v.number()),
    }),
    branding: v.any(),
    integrations: v.any(),
    createdAt: v.number(),
    updatedAt: v.number(),
  })
    .index("by_slug", ["slug"]),

  products: defineTable({
    storeId: v.id("stores"),
    categoryId: v.id("categories"),
    name: v.string(),
    slug: v.string(),
    // ... other fields
  })
    .index("by_store", ["storeId"])
    .index("by_store_slug", ["storeId", "slug"]),

  // ... other tables
})
```

### Step 4: Use it in your Next.js app

#### In a client component

```typescript
"use client"

import { useQuery, useMutation } from "convex/react"
import { api } from "@/convex/_generated/api"

export function StoresList() {
  const stores = useQuery(api.stores.list)
  const createStore = useMutation(api.stores.create)

  const handleCreate = async () => {
    await createStore({
      name: "Mon Restaurant",
      slug: "mon-restaurant",
      address: {
        street: "123 Rue Example",
        city: "Paris",
        postalCode: "75001",
        country: "France",
      },
      settings: {
        currency: "EUR",
        timezone: "Europe/Paris",
        deliveryEnabled: true,
        pickupEnabled: true,
        dineInEnabled: true,
      },
    })
  }

  if (!stores) return <div>Chargement...</div>

  return (
    <div>
      <button onClick={handleCreate}>Créer un store</button>
      <ul>
        {stores.map((store) => (
          <li key={store._id}>{store.name}</li>
        ))}
      </ul>
    </div>
  )
}
```

#### In a Server Component (Next.js 15+)

```typescript
import { fetchQuery } from "convex/nextjs"
import { api } from "@/convex/_generated/api"

export default async function StoresPage() {
  const stores = await fetchQuery(api.stores.list)

  return (
    <div>
      <h1>Stores</h1>
      <ul>
        {stores.map((store) => (
          <li key={store._id}>{store.name}</li>
        ))}
      </ul>
    </div>
  )
}
```

#### In an API Route

```typescript
import { fetchMutation } from "convex/nextjs"
import { api } from "@/convex/_generated/api"

export async function POST(request: Request) {
  const data = await request.json()

  const storeId = await fetchMutation(api.stores.create, {
    name: data.name,
    slug: data.slug,
    // ... other fields
  })

  return Response.json({ storeId })
}
```

## Usage examples

### Create an order with automatic total calculation

```typescript
const createOrder = useMutation(api.orders.create)

await createOrder({
  storeId: store._id,
  customer: {
    name: "John Doe",
    email: "john@example.com",
    phone: "+33612345678",
  },
  items: [
    {
      productId: product._id,
      name: "Pizza Margherita",
      quantity: 2,
      price: 12.90,
      selectedOptions: [
        {
          optionId: "size",
          optionName: "Taille",
          choiceId: "large",
          choiceName: "Grande",
          price: 2.00,
        },
      ],
    },
  ],
  type: "delivery",
  deliveryAddress: {
    street: "123 Rue Example",
    city: "Paris",
    postalCode: "75001",
    country: "France",
  },
  paymentMethod: "stripe",
})
// The subtotal, taxAmount, deliveryFee and total fields are computed automatically
```

### Manage kitchen tickets

```typescript
const tickets = useQuery(api.kitchenTickets.getByStatus, {
  storeId: store._id,
  status: "pending",
})

const updateStatus = useMutation(api.kitchenTickets.updateStatus)

// Start preparation
await updateStatus({
  id: ticket._id,
  status: "in_progress",
})
// startedAt is set automatically

// Mark as ready
await updateStatus({
  id: ticket._id,
  status: "ready",
})
// completedAt is set automatically
```

### Manage translations

```typescript
const upsert = useMutation(api.translations.upsert)

// Create/update a translation
await upsert({
  storeId: store._id,
  entityType: "product",
  entityId: product._id,
  field: "name",
  languageCode: "fr",
  value: "Pizza Margherita",
  isAutoTranslated: false,
})

// Bulk upsert
const bulkUpsert = useMutation(api.translations.bulkUpsert)

await bulkUpsert({
  translations: [
    {
      storeId: store._id,
      entityType: "product",
      entityId: product._id,
      field: "name",
      languageCode: "es",
      value: "Pizza Margarita",
      isAutoTranslated: true,
    },
    {
      storeId: store._id,
      entityType: "product",
      entityId: product._id,
      field: "description",
      languageCode: "es",
      value: "Tomate, mozzarella, albahaca",
      isAutoTranslated: true,
    },
  ],
})
```

### Manage languages

```typescript
const setDefault = useMutation(api.languages.setDefault)

// Set French as the default language
// Every other language is automatically set to isDefault: false
await setDefault({
  storeId: store._id,
  languageId: frenchLanguage._id,
})
```

## Best practices

### 1. Always filter by storeId

```typescript
// ✅ Good
const products = useQuery(api.products.list, { storeId: store._id })

// ❌ Bad (don't query without a store filter)
```

### 2. Handle loading states

```typescript
const stores = useQuery(api.stores.list)

if (stores === undefined) {
  return <LoadingSpinner />
}

if (stores.length === 0) {
  return <EmptyState />
}

return <StoresList stores={stores} />
```

### 3. Handle errors

```typescript
const createStore = useMutation(api.stores.create)

try {
  await createStore({ ... })
  toast.success("Store créé")
} catch (error) {
  toast.error("Erreur lors de la création")
  console.error(error)
}
```

### 4. Use the helpers

```typescript
import { generateOrderNumber, generateSlug } from "@/convex/helpers"

// In a custom mutation
const orderNumber = generateOrderNumber() // "ORD-2026-ABC123"
const slug = generateSlug("Mon Super Produit") // "mon-super-produit"
```

## Tests

### Testing mutations

```typescript
import { test, expect } from "vitest"
import { ConvexTestingHelper } from "convex-test"
import { api } from "./_generated/api"

test("create store", async () => {
  const t = new ConvexTestingHelper()

  const storeId = await t.mutation(api.stores.create, {
    name: "Test Store",
    slug: "test-store",
    // ... other fields
  })

  expect(storeId).toBeDefined()

  const store = await t.query(api.stores.getById, { id: storeId })
  expect(store?.name).toBe("Test Store")
})
```

## Migrating from a previous version

If you already have your own functions, here is how to migrate:

1. Back up your custom functions
2. Copy the new functions from `convex-functions`
3. Re-integrate your custom functions
4. Test that everything works

## Support

For any question or issue:
- Check the Convex documentation: https://docs.convex.dev
- Check the schema in the `convex-schema` package
- Look at the examples in this file
